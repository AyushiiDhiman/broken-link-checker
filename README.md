# 🔗 Broken Link Automation Checker

A Python-based automation project that scans any website for broken links using **Selenium**, **Requests**, **PyTest**, **Pandas**, and **Jinja2**.  
This tool opens a webpage, collects all anchor tags `<a>`, checks the HTTP response of each URL, and generates both **HTML** and **CSV** reports.

---

## ✅ Features

- ✅ Automates browser using Selenium WebDriver  
- ✅ Extracts all links from any given webpage  
- ✅ Detects **broken**, **working**, and **timeout** links  
- ✅ Generates a **CSV report**  
- ✅ Generates a **color-coded HTML report**  
- ✅ Fully automated using PyTest  
- ✅ Can test any website by passing URL from command line  

---


